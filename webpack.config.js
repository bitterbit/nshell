const path = require('path');

module.exports = {
    //...
    resolve: {
        alias: {
            // configuration options
            // 'vorpal-less': require.resolve('node_modules/vorpal-less/dist'),
            // 'vorpal-grep': require.resolve('node_modules/vorpal-grep')
        }
    }
  };